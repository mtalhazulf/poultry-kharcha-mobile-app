package com.mps.expensetracker.widget

import com.reactnativeandroidwidget.RNWidgetProvider

/** Home-screen widget (quick-add / recent-entries); rendering lives in src/widgets. */
class MpsExpenseWidget : RNWidgetProvider()
