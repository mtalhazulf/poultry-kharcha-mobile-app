<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Biometric sign-in (react-native-keychain -> BiometricPrompt). Install-time
         permission; the library also merges USE_FINGERPRINT for API < 28. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Receipt capture (react-native-image-picker launchCamera). Runtime-requested. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Gallery selection goes through the Android Photo Picker, which needs
         no storage permission (and keeps us out of Play's photo/video
         permission policy). -->

    <!-- The app still works (gallery only) on devices without a camera. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- Android 11+ package visibility: lets Linking.canOpenURL / openURL
         resolve a browser for the Supabase OAuth authorize URL. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- OAuth deep link: Supabase redirects to kharcha://auth/callback
             after browser-based sign-in. singleTask above means the existing
             activity receives it via onNewIntent instead of a second instance.
             Must match OAUTH_REDIRECT_URL in .env and the Supabase
             Authentication -> URL Configuration -> Redirect URLs list. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data
                android:scheme="kharcha"
                android:host="auth"
                android:pathPrefix="/callback" />
        </intent-filter>
      </activity>
    </application>
</manifest>
